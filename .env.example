# Touchpoint currently requires no runtime environment variables.
# Keep this file as the only tracked .env variant.
# Never place provider credentials, tokens or production configuration here.

# Add reviewed placeholder-only entries if a future server-side integration is introduced.
